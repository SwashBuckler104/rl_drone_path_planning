# Reinforcement Learning framework
gymnasium>=0.29.1,<1.0.0
# RL algorithms (includes PyTorch as a dependency)
stable-baselines3[extra]
# Numerical computing
numpy>=1.24.0,<2.0.0
# Computer vision (for map loading / frame processing)
opencv-python>=4.8.0,<5.0.0
# Plotting and animation
matplotlib>=3.7.0,<4.0.0
# Image processing (for GIF animation output)
Pillow>=10.0.0,<12.0.0
# Progress bars (used internally by SB3 and optionally by scripts)
tqdm>=4.65.0
tensorboard
